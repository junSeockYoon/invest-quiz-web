# 투자 퀴즈 시스템 기술 스택 및 구현 상세 문서

## 📋 개요
투자 퀴즈 시스템은 챕터별 퀴즈 풀이, 즉시 피드백, 실시간 진행률 추적, 상세한 성능 분석 기능을 제공하는 웹 애플리케이션입니다. 사용자는 원하는 챕터를 선택하여 퀴즈를 풀고, 각 문제마다 즉시 정답 여부를 확인할 수 있습니다.

---

## 🛠 기술 스택

### **백엔드 (Backend)**

#### **1. 런타임 환경**
- **Node.js**: JavaScript 서버 사이드 런타임 환경
- **Express.js 5.1.0**: 웹 애플리케이션 프레임워크
  - RESTful API 설계
  - 미들웨어 기반 요청 처리
  - 라우팅 시스템
  - 포트: 3001

#### **2. 데이터베이스**
- **MySQL**: 관계형 데이터베이스 관리 시스템
- **MySQL2 (v3.14.5)**: Promise 기반 MySQL 클라이언트
  - Connection Pool 사용 (최대 30개 연결)
  - 비동기 쿼리 처리
  - 데이터베이스: `invest_dev`

#### **3. ORM/쿼리 빌더**
- **MyBatis Mapper (v0.8.0)**: XML 기반 SQL 매핑
  - SQL과 코드의 완전한 분리
  - 재사용 가능한 쿼리 관리
  - 동적 SQL 지원
  - 네임스페이스 기반 쿼리 관리

#### **4. 템플릿 엔진**
- **EJS (v3.1.10)**: Embedded JavaScript 템플릿
  - 서버 사이드 렌더링 (SSR)
  - 동적 HTML 생성
  - 서버 데이터를 클라이언트로 전달

#### **5. 로깅 시스템**
- **Winston (v3.11.0)**: 로그 관리 라이브러리
- **Winston Daily Rotate File (v4.7.1)**: 일별 로그 파일 자동 생성
  - Info/Error 레벨 분리
  - 30일 보관 정책
  - 자동 압축 기능 (gzip)
  - 개발 환경에서 콘솔 출력

#### **6. 보안 및 유틸리티**
- **bcrypt (v6.0.0)**: 비밀번호 해싱
- **cookie-parser (v1.4.7)**: 쿠키 파싱
- **uuid (v9.0.1)**: 고유 식별자 생성
- **moment (v2.30.1)**: 날짜/시간 처리
- **lodash (v4.17.21)**: 유틸리티 함수 라이브러리 (snake_case → camelCase 변환)
- **multer (v1.4.5-lts.1)**: 파일 업로드 처리
- **mobile-detect (v1.4.5)**: 모바일 디바이스 감지

---

### **프론트엔드 (Frontend)**

#### **1. 핵심 기술**
- **Vanilla JavaScript (ES6+)**: 프레임워크 없이 순수 JavaScript 사용
  - 모듈화된 함수 구조
  - 이벤트 기반 프로그래밍
  - Fetch API를 통한 비동기 통신
  - 클로저를 활용한 상태 관리

#### **2. 스타일링**
- **Tailwind CSS (CDN)**: 유틸리티 우선 CSS 프레임워크
  - 반응형 디자인
  - 모바일 최적화 (최대 너비 480px)
  - 커스텀 애니메이션 (페이드 인/아웃, 진행 바, 모달)
  - 그라데이션 배경 효과

#### **3. 사용자 인터랙션**
- 실시간 진행률 표시 (Progress Bar)
- 페이드 전환 애니메이션 (300ms)
- 버튼 호버/클릭 피드백
- 즉시 피드백 시스템 (정답/오답 모달)
- 자동 다음 문제 이동 (정답 시 500ms 딜레이)

---

## 🏗 아키텍처 패턴

### **MVC (Model-View-Controller) 패턴**

```
HTTP Request
    ↓
Controller (컨트롤러) - 요청 처리 및 응답
    ↓
Service (서비스 로직) - 비즈니스 로직 및 데이터 가공
    ↓
DAO (Data Access Object) - 데이터베이스 접근
    ↓
MyBatis Mapper (XML) - SQL 쿼리 실행
    ↓
Database (MySQL) - 데이터 저장/조회
```

### **레이어별 상세 구현**

#### **1. Controller Layer** (`src/controllers/test.controller.js`)

**역할**: HTTP 요청/응답 처리, 클라이언트와 서버 간 인터페이스

**구현된 함수들**:

1. **`main(req, res)`**
   - 경로: `GET /test`
   - 기능: 챕터 선택 화면으로 리다이렉트
   - 구현: `res.redirect('/test/chapters')`

2. **`showChapters(req, res)`**
   - 경로: `GET /test/chapters`
   - 기능: 챕터 선택 화면 렌더링
   - 구현: `res.render('test/chapters')`

3. **`getQuestionsByChapter(req, res)`**
   - 경로: `GET /test/chapter/:chapterId`
   - 기능: 특정 챕터의 문제 목록 조회 및 퀴즈 화면 렌더링
   - 파라미터: `req.params.chapterId`
   - 구현: 서비스에서 문제 조회 후 `res.render('test/main', { result })`

4. **`getChapters(req, res)`**
   - 경로: `GET /test/api/chapters`
   - 기능: 챕터 목록 조회 (JSON API)
   - 응답: JSON 형식의 챕터 목록

5. **`submitResult(req, res)`**
   - 경로: `POST /test/submit`
   - 기능: 퀴즈 결과 저장
   - 요청 본문: `req.body` (JSON)
   - 응답: 성공/실패 상태와 함께 JSON 응답

#### **2. Service Layer** (`src/services/test.services.js`)

**역할**: 비즈니스 로직 처리, 데이터 검증 및 가공, 트랜잭션 관리

**구현된 함수들**:

1. **`submitResult(resultData)`**
   - **기능**: 퀴즈 결과를 데이터베이스에 저장
   - **처리 과정**:
     1. 마지막 유저 번호 조회 (`getLastUserNumber`)
     2. 다음 유저 번호 계산 및 사용자 이름 생성 (`USER_N` 형식)
     3. 전체 결과 저장 (`insertResult`)
        - `user_name`: 자동 생성된 사용자 이름
        - `total_score`: 정답 개수
        - `chapter_id`: 챕터 ID
        - `grade`: 빈 문자열 (사용하지 않음)
     4. 생성된 `resultId` 확인
     5. 각 문제별 상세 결과 저장 (`insertDetail` 반복)
        - `result_id`: 위에서 생성된 결과 ID
        - `question_num`: 문제 번호
        - `user_answer`: 사용자 답변 ("O" 또는 "X")
        - `is_correct`: 정답 여부 (1 또는 0)
        - `response_time_ms`: 응답 시간 (밀리초)
   - **에러 처리**: null/undefined 값 필터링, 로깅

2. **`getChapters()`**
   - **기능**: 모든 챕터 목록 조회
   - **반환 데이터**:
     - `id`: 챕터 ID
     - `chapter_title`: 챕터 제목
     - `chapter_description`: 챕터 설명
     - `question_count`: 해당 챕터의 문제 수

3. **`getQuestionsByChapter(chapterId)`**
   - **기능**: 특정 챕터의 문제 목록 조회
   - **반환 데이터**:
     - `question_num`: 문제 번호
     - `content`: 문제 내용
     - `answer`: 정답 ("O" 또는 "X")
     - `chapter_title`: 챕터 제목
     - `chapter_description`: 챕터 설명

#### **3. DAO Layer** (`src/dao/common.dao.js`)

**역할**: 데이터베이스 접근 추상화, MyBatis Mapper를 통한 SQL 실행

**구현 상세**:

1. **MyBatis Mapper 초기화**
   ```javascript
   mybatisMapper.createMapper([
       __dirname + '/mapper/main.xml',
       __dirname + '/mapper/test.xml',
   ]);
   ```

2. **`commonDao(mapperId, sqlId, params)`**
   - **기능**: 공통 데이터 접근 함수
   - **처리 과정**:
     1. MyBatis Mapper에서 SQL 쿼리 가져오기
     2. 개발 환경에서 SQL 로그 출력
     3. MySQL Connection Pool을 통한 쿼리 실행
     4. 결과 처리:
        - INSERT/UPDATE/DELETE: `affectedRows` 반환
        - SELECT: `convertCamelcase()`를 통한 snake_case → camelCase 변환
   - **에러 처리**: Winston 로거를 통한 에러 로깅

3. **데이터 변환**
   - `convertCamelcase()`: DB의 snake_case 컬럼명을 JavaScript의 camelCase로 변환
   - 예: `chapter_title` → `chapterTitle`

#### **4. Model Layer** (`src/models/common/`)

**역할**: 데이터 구조 정의, API 응답 포맷 표준화

**구현된 모델**:
- `result.model.js`: 표준화된 API 응답 형식
  - `info`: 상태 정보 (status, message)
  - `dataset`: 실제 데이터

---

## 📊 주요 기능 구현 상세

### **1. 챕터 선택 시스템**

#### **구현 위치**
- **뷰**: `view/test/chapters.ejs`
- **컨트롤러**: `testController.showChapters()`, `testController.getChapters()`
- **서비스**: `testService.getChapters()`
- **SQL**: `getChapters` 쿼리

#### **동작 흐름**
1. 사용자가 `/test/chapters` 접속
2. 서버에서 EJS 템플릿 렌더링
3. 클라이언트에서 `GET /test/api/chapters` API 호출
4. 서버에서 챕터 목록 조회 (각 챕터의 문제 수 포함)
5. JavaScript로 동적으로 챕터 카드 생성
6. 챕터 클릭 시 `/test/chapter/:chapterId`로 이동

#### **SQL 쿼리 상세**
```sql
SELECT 
    c.id,
    c.title AS chapter_title,
    c.description AS chapter_description,
    COUNT(q.id) AS question_count
FROM chapters c
LEFT JOIN questions q ON c.id = q.chapter_id
GROUP BY c.id, c.title, c.description
ORDER BY c.id ASC
```

### **2. 퀴즈 풀이 시스템**

#### **구현 위치**
- **뷰**: `view/test/main.ejs`
- **JavaScript**: `public/js/quiz.js`
- **컨트롤러**: `testController.getQuestionsByChapter()`
- **서비스**: `testService.getQuestionsByChapter()`
- **SQL**: `getQuestionsByChapter` 쿼리

#### **상태 관리**
```javascript
let quizData = {
    questions: [],           // 문제 배열
    currentIndex: 0,         // 현재 문제 인덱스
    answers: [],             // 사용자 답변 배열
    totalQuestions: 0,      // 전체 문제 수
    startTime: null,         // 퀴즈 시작 시각
    questionStartTime: null, // 현재 문항 시작 시각
    chapterId: null          // 현재 챕터 ID
};
```

#### **핵심 함수들**

1. **`initQuiz(questions)`**
   - 퀴즈 데이터 초기화
   - 시작 시간 기록
   - 첫 번째 문제 표시

2. **`displayQuestion(index)`**
   - 특정 인덱스의 문제 표시
   - 문항 시작 시각 기록 (응답 시간 측정용)
   - 챕터 정보 업데이트
   - 버튼 상태 초기화

3. **`selectAnswer(answer)`**
   - 사용자가 선택한 답변 처리
   - 응답 시간 계산 (밀리초)
   - 정답 여부 즉시 확인
   - 정답: 초록색 피드백 후 자동 다음 문제 이동
   - 오답: 빨간색 피드백 후 오답 모달 표시

4. **`moveToNextQuestion()`**
   - 페이드 아웃 애니메이션 (300ms)
   - 다음 문제 표시 또는 완료 처리

5. **`completeQuiz()`**
   - 정답 개수 및 정답률 계산
   - 전체 소요 시간 계산 (초 단위)
   - 서버로 결과 전송 (`POST /test/submit`)
   - 완료 모달 표시

#### **SQL 쿼리 상세**
```sql
SELECT 
    q.question_num, 
    q.content, 
    q.answer, 
    c.title AS chapter_title, 
    c.description AS chapter_description
FROM questions q
JOIN chapters c ON q.chapter_id = c.id
WHERE c.id = #{chapterId}
ORDER BY q.question_num ASC
```

### **3. 즉시 피드백 시스템**

#### **구현 위치**
- **JavaScript**: `public/js/quiz.js` (showCorrectFeedback, showIncorrectFeedback)
- **뷰**: `view/test/main.ejs` (오답 모달, 완료 모달)

#### **정답 피드백**
1. 버튼 색상 변경: 파란색/빨간색 → 초록색
2. 아이콘 변경: "O"/"X" → "✓"
3. 텍스트 변경: "정답"/"오답" → "정답입니다!"
4. 500ms 후 자동으로 다음 문제로 이동

#### **오답 피드백**
1. 버튼 색상 변경: 파란색/빨간색 → 빨간색
2. 아이콘 변경: "O"/"X" → "✗"
3. 텍스트 변경: "정답"/"오답" → "오답입니다"
4. 오답 모달 팝업:
   - "오답입니다" 메시지
   - 정답 표시 ("정답: O" 또는 "정답: X")
   - "다음 문제" 버튼
5. 사용자가 "다음 문제" 클릭 시 다음 문제로 이동

#### **완료 모달**
- 모든 문제 완료 시 표시
- 표시 정보:
  - 정답 개수 / 전체 문제 수
  - 정답률 (%)
  - 소요 시간 (초)
- "챕터 선택으로 돌아가기" 버튼

### **4. 데이터 저장 시스템**

#### **구현 위치**
- **컨트롤러**: `testController.submitResult()`
- **서비스**: `testService.submitResult()`
- **SQL**: `insertResult`, `insertDetail` 쿼리

#### **저장 과정 상세**

1. **전체 결과 저장** (`test_results` 테이블)
   ```sql
   INSERT INTO test_results (user_name, total_score, chapter_id, grade)
   VALUES (#{userName}, #{totalScore}, #{chapterId}, '')
   ```
   - `user_name`: 자동 생성 (`USER_1`, `USER_2`, ...)
   - `total_score`: 정답 개수
   - `chapter_id`: 챕터 ID
   - `grade`: 빈 문자열 (사용하지 않음)

2. **마지막 유저 번호 조회**
   ```sql
   SELECT COALESCE(MAX(CAST(SUBSTRING(user_name, 6) AS UNSIGNED)), 0) AS last_number
   FROM test_results
   WHERE user_name LIKE 'USER_%'
   ```
   - `USER_` 접두사를 제거하고 숫자 부분만 추출
   - 최대값을 찾아 다음 번호 생성

3. **각 문제별 상세 결과 저장** (`test_details` 테이블)
   ```sql
   INSERT INTO test_details (result_id, question_num, user_answer, is_correct, response_time_ms)
   VALUES (#{resultId}, #{questionNum}, #{userAnswer}, #{isCorrect}, #{responseTimeMs})
   ```
   - 반복문을 통해 각 문제마다 개별 INSERT 실행
   - null/undefined 값 필터링

#### **데이터 구조**
```javascript
// 프론트엔드에서 전송하는 데이터
{
    totalScore: 5,              // 정답 개수
    elapsedSeconds: 120,        // 전체 소요 시간 (초)
    chapterId: 1,               // 챕터 ID
    details: [
        {
            questionNum: 1,
            userAnswer: "O",
            isCorrect: 1,
            responseTimeMs: 2500
        },
        // ... 나머지 문제들
    ]
}
```

### **5. 시간 측정 시스템**

#### **전체 퀴즈 소요 시간**
- **측정 시점**: `initQuiz()` 호출 시 `startTime` 기록
- **종료 시점**: `completeQuiz()` 호출 시 `endTime` 기록
- **계산**: `Math.round((endTime - startTime) / 1000)` (초 단위)

#### **문항별 응답 시간**
- **측정 시점**: `displayQuestion()` 호출 시 `questionStartTime` 기록
- **종료 시점**: `selectAnswer()` 호출 시 현재 시간 기록
- **계산**: `now - questionStartTime` (밀리초 단위)
- **저장**: 각 문제의 `responseTimeMs`에 저장

### **6. 에러 처리 및 방어 코드**

#### **프론트엔드**
- null/undefined 값 필터링: `quizData.answers.filter(answer => !!answer)`
- 빈 값 체크: `if (!questions || questions.length === 0)`
- 버튼 비활성화: 중복 클릭 방지

#### **백엔드**
- null 체크: `if (!detail || detail.questionNum == null) continue`
- 에러 로깅: Winston을 통한 체계적인 에러 추적
- 표준화된 에러 응답: `ResultModel`을 통한 일관된 형식

---

## 🔧 개발 환경 설정

### **서버 설정** (`app.js`)

```javascript
// 포트 설정
const port = 3001;

// 뷰 엔진 설정
app.set('views', path.join(__dirname, 'view'));
app.set('view engine', 'ejs');

// 미들웨어 설정
app.use(express.json());                    // JSON 파싱
app.use(express.urlencoded({ extended: true })); // URL 인코딩 파싱
app.use(express.static(path.join(__dirname, 'public'))); // 정적 파일
app.use(cookieParser());                    // 쿠키 파싱
app.use('/', routes);                       // 라우터 등록
```

### **데이터베이스 설정** (`src/config/database.js`)

```javascript
const pool = mysql.createPool({
    host: config.host,
    port: 3306,
    user: 'root',
    password: 'qwer1234@',
    database: 'invest_dev',
    connectionLimit: 30
});
```

### **로깅 설정** (`src/config/logger.js`)

- Info 레벨: `logs/info/YYYY-MM-DD.info.log`
- Error 레벨: `logs/error/YYYY-MM-DD.error.log`
- 보관 기간: 30일
- 자동 압축: gzip
- 개발 환경: 콘솔 출력

### **실행 명령어**
```bash
# 개발 모드 (nodemon 사용)
npm run dev

# 프로덕션 모드
npm start
```

---

## 📁 프로젝트 구조

```
invest-quiz-web/
├── app.js                      # Express 앱 진입점
├── package.json                # 의존성 관리
├── src/
│   ├── config/                 # 설정 파일
│   │   ├── database.js         # MySQL 연결 설정
│   │   ├── logger.js           # Winston 로거 설정
│   │   ├── app.config.js       # 앱 설정
│   │   └── color.config.js     # 콘솔 색상 설정
│   ├── controllers/            # 컨트롤러 (요청 처리)
│   │   ├── main.controller.js  # 메인 컨트롤러
│   │   └── test.controller.js  # 퀴즈 컨트롤러
│   ├── services/               # 서비스 로직
│   │   ├── index.js            # 서비스 인덱스
│   │   ├── main.services.js    # 메인 서비스
│   │   └── test.services.js    # 퀴즈 서비스
│   ├── dao/                    # 데이터 접근 계층
│   │   ├── common.dao.js       # 공통 DAO 함수
│   │   └── mapper/             # MyBatis XML 매퍼
│   │       ├── main.xml        # 메인 매퍼
│   │       └── test.xml        # 퀴즈 매퍼
│   ├── models/                 # 데이터 모델
│   │   └── common/
│   │       ├── result.model.js      # 결과 모델
│   │       ├── resultData.model.js  # 결과 데이터 모델
│   │       ├── resultError.model.js # 에러 모델
│   │       └── resultInfo.model.js   # 정보 모델
│   ├── routes/                 # 라우팅 정의
│   │   ├── index.js            # 라우트 인덱스
│   │   ├── main.route.js       # 메인 라우트
│   │   └── test.route.js       # 퀴즈 라우트
│   └── common/                 # 공통 유틸리티
│       └── utils/
│           ├── common.util.js  # 공통 유틸리티 (camelCase 변환 등)
│           ├── crypto.util.js  # 암호화 유틸리티
│           ├── date.util.js     # 날짜 유틸리티
│           ├── email.util.js    # 이메일 유틸리티
│           └── sessionStorage.util.js # 세션 스토리지 유틸리티
├── view/                       # EJS 템플릿 파일
│   ├── main.ejs                # 메인 랜딩 페이지
│   └── test/
│       ├── chapters.ejs        # 챕터 선택 화면
│       └── main.ejs            # 퀴즈 화면
├── public/                     # 정적 파일
│   └── js/
│       └── quiz.js             # 퀴즈 프론트엔드 로직
└── logs/                       # 로그 파일 저장소
    ├── info/                   # Info 레벨 로그
    └── error/                  # Error 레벨 로그
```

---

## 📈 데이터베이스 스키마

### **주요 테이블**

#### **1. `chapters` (챕터)**
- `id`: 챕터 ID (PK, Auto Increment)
- `title`: 챕터 제목
- `description`: 챕터 설명

#### **2. `questions` (문제)**
- `id`: 문제 ID (PK, Auto Increment)
- `question_num`: 문제 번호
- `content`: 문제 내용
- `answer`: 정답 ("O" 또는 "X")
- `chapter_id`: 챕터 ID (FK → chapters.id)

#### **3. `test_results` (퀴즈 결과)**
- `id`: 결과 ID (PK, Auto Increment)
- `user_name`: 사용자 이름 (USER_N 형식)
- `total_score`: 총 정답 개수
- `chapter_id`: 챕터 ID (FK → chapters.id)
- `grade`: 등급 (현재는 빈 문자열로 저장, 사용하지 않음)
- `created_at`: 생성 시각 (자동)

#### **4. `test_details` (문항별 상세 결과)**
- `result_id`: 결과 ID (FK → test_results.id)
- `question_num`: 문제 번호
- `user_answer`: 사용자 답변 ("O" 또는 "X")
- `is_correct`: 정답 여부 (1: 정답, 0: 오답)
- `response_time_ms`: 응답 시간 (밀리초)

### **관계도**
```
chapters (1) ──< (N) questions
chapters (1) ──< (N) test_results
test_results (1) ──< (N) test_details
```

---

## 🎯 기술적 특징 및 최적화

### **1. 성능 최적화**
- **Connection Pool**: 데이터베이스 연결 재사용 (최대 30개)
- **비동기 처리**: Promise 기반 비동기 프로그래밍
- **정적 파일 캐싱**: Express static 미들웨어 활용
- **SQL 최적화**: JOIN을 통한 효율적인 데이터 조회

### **2. 확장성**
- **모듈화된 구조**: 기능별 파일 분리
- **재사용 가능한 DAO**: 공통 데이터 접근 함수
- **표준화된 응답 모델**: 일관된 API 응답 형식
- **챕터 기반 구조**: 새로운 챕터 추가 용이

### **3. 유지보수성**
- **로깅 시스템**: 체계적인 에러 추적 (Winston)
- **코드 주석**: 명확한 설명 제공
- **표준 패턴**: MVC 아키텍처 준수
- **SQL 분리**: MyBatis를 통한 SQL과 코드 분리

### **4. 사용자 경험**
- **반응형 디자인**: 모바일 최적화 (최대 너비 480px)
- **부드러운 애니메이션**: 페이드 전환 효과 (300ms)
- **즉시 피드백**: 정답/오답 즉시 확인
- **실시간 진행률**: 진행 바를 통한 시각적 피드백
- **직관적인 UI**: Tailwind CSS를 통한 현대적인 디자인

### **5. 데이터 분석 기능**
- **문항별 응답 시간**: 밀리초 단위 정밀 측정
- **전체 소요 시간**: 초 단위 계산
- **정답률 계산**: 정답 개수 / 전체 문제 수
- **챕터별 결과 추적**: chapter_id를 통한 챕터별 분석 가능

---

## 🚀 API 엔드포인트 상세

### **View 라우트 (EJS 렌더링)**

#### **GET /test**
- **기능**: 챕터 선택 화면으로 리다이렉트
- **응답**: 302 Redirect → `/test/chapters`

#### **GET /test/chapters**
- **기능**: 챕터 선택 화면 렌더링
- **응답**: HTML (EJS 템플릿)
- **템플릿**: `view/test/chapters.ejs`

#### **GET /test/chapter/:chapterId**
- **기능**: 특정 챕터의 퀴즈 화면 렌더링
- **파라미터**: `chapterId` (URL 파라미터)
- **응답**: HTML (EJS 템플릿)
- **템플릿**: `view/test/main.ejs`
- **데이터**: 해당 챕터의 문제 목록

### **API 라우트 (JSON 응답)**

#### **GET /test/api/chapters**
- **기능**: 챕터 목록 조회
- **응답**: JSON
- **응답 형식**:
```json
{
    "info": {
        "status": "S",
        "message": null
    },
    "dataset": {
        "data": [
            {
                "id": 1,
                "chapterTitle": "환율 (Exchange Rate)",
                "chapterDescription": "경제의 온도를 조절하는 '돈의 가격'입니다",
                "questionCount": 5
            },
            // ... 나머지 챕터들
        ]
    }
}
```

#### **POST /test/submit**
- **기능**: 퀴즈 결과 저장
- **요청 본문**: JSON
- **요청 형식**:
```json
{
    "totalScore": 5,
    "elapsedSeconds": 120,
    "chapterId": 1,
    "details": [
        {
            "questionNum": 1,
            "userAnswer": "O",
            "isCorrect": 1,
            "responseTimeMs": 2500
        },
        // ... 나머지 문제들
    ]
}
```
- **응답**: JSON
- **응답 형식**:
```json
{
    "info": {
        "status": "S",
        "message": null
    },
    "dataset": {
        "data": {
            "resultId": 123,
            "userName": "USER_1"
        }
    }
}
```

---

## 📝 결론

본 퀴즈 시스템은 **Node.js + Express.js** 기반의 안정적이고 확장 가능한 웹 애플리케이션으로, **MVC 아키텍처**를 통해 체계적인 코드 구조를 유지하고 있습니다. 

**주요 특징**:
- **챕터별 퀴즈 시스템**: 사용자가 원하는 챕터를 선택하여 학습 가능
- **즉시 피드백**: 정답/오답을 바로 확인하여 학습 효과 극대화
- **상세한 성능 분석**: 문항별 응답 시간 및 전체 소요 시간 측정
- **MyBatis Mapper**를 활용한 SQL 관리로 유지보수성 향상
- **Winston** 기반 로깅 시스템으로 운영 안정성 확보
- **Vanilla JavaScript**와 **Tailwind CSS**를 활용한 경량 프론트엔드로 빠른 사용자 경험 제공

이 시스템은 투자 교육을 위한 효과적인 학습 도구로 활용될 수 있으며, 향후 추가 기능 확장이 용이한 구조로 설계되었습니다.